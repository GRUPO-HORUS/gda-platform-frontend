import { RolModel } from "../modules/auth/_models/rol.model";

export class RolesTabla {
  content: RolModel[];
  empty: boolean;
  
  /*setRolesTabla(error: any) {
    this.message = error.message || '';
    this.formatted = error.formatted; 
  }*/
}
