import { UnidadModel } from "../pages/ubicacion/model/unidad.model";

export class UnidadesTabla {
  content: UnidadModel[];
  empty: boolean;
  totalElements: number;
  
  /*setBienesTabla(error: any) {
    this.message = error.message || '';
    this.formatted = error.formatted; 
  }*/
}
